import { AppConfig } from 'services';
const route = () => {
  console.log('RUNNING');
  AppConfig.validateConfigs();
};

// add to favourite

// remove from favourite

// register new user

// delete user

// login
export default route;
